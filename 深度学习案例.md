# 深度学习与计算机视觉案例

下面每个案例都写了「做了什么」和「关键知识点」，方便日后复习。

## Softmax 手写数字识别

- 文件：[softmax_mnist_demo.ipynb](softmax_mnist_demo.ipynb)
- **做了什么**：用全连接神经网络（Dense 层）+ Softmax 识别 MNIST 手写数字（0~9）。从读数据、观察 28×28 像素矩阵，到训练和测试准确率，一步步讲解。
- **知识点**：Softmax 多分类、one-hot 编码（`to_categorical`）、交叉熵损失、全连接层 Dense、Dropout、Flatten、训练/测试准确率。

## Fashion-MNIST 服饰分类（CNN）

- 文件：[fashion_mnist_cnn_teaching_case.ipynb](fashion_mnist_cnn_teaching_case.ipynb)
- **做了什么**：用卷积神经网络（`Conv2D` + `MaxPooling2D` + `Dense`）对 10 类服饰图片分类。
- **知识点**：卷积层（局部感受野 / 权重共享）、池化层（降采样）、特征图、全连接层、Dropout、Softmax。

## CIFAR-10 图像分类（CNN + 数据增强）

- 文件：[cifar10_cnn_teaching_case.ipynb](cifar10_cnn_teaching_case.ipynb)
- **做了什么**：在 CIFAR-10 彩色图片（32×32）上训练更深的 CNN，加入 BatchNorm 和数据增强，观察验证集准确率。
- **知识点**：BatchNorm、数据增强（翻转 / 平移）、过拟合与缓解、彩色三通道输入、硬件加速。

## MediaPipe 关键点识别

- 目录：[media_pipe/](media_pipe/)
- **做了什么**：手部关键点、人脸关键点 + 表情（BlendShapes）、身体骨骼姿态、全身 Holistic 综合识别。
- **知识点**：关键点检测、landmarker 模型（`.task`）、姿态估计、表情捕捉。

## OpenCV 传统图像处理

- 目录：[opencv/](opencv/)
- **做了什么**：一个丰富的传统图像处理教学案例，覆盖灰度、模糊、边缘检测、阈值分割、形态学、轮廓检测等。
- **知识点**：传统视觉处理流程（模糊 → 边缘 → 阈值 → 形态学 → 轮廓）、高斯模糊、Canny 边缘检测。

## YOLO 目标检测

- 目录：[yolo/](yolo/)
- **做了什么**：用 YOLOv8 完成图片、视频、实时摄像头的目标检测。
- **知识点**：目标检测（分类 + 定位）、置信度阈值、FPS、模型大小权衡（yolov8n / s / m）、逐帧检测。
